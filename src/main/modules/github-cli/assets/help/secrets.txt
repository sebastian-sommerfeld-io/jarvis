Secrets for all repositories
  GH_TOKEN_REPO_AND_PROJECT          Pipeline interacts with Github Projects
  GOOGLE_CHAT_WEBHOOK                Pipeline sends error messages to chat

Secrets for repositories with Docker images
  DOCKERHUB_USER                     Deploy container image to DockerHub
  DOCKERHUB_PASS                     Deploy container image to DockerHub
  SNYK_TOKEN                         Scan container image for security issues

Secrets for repositories with HTML websites
  FTP_USER                           Upload html contents to ftp webspace
  FTP_PASS                           Upload html contents to ftp webspace
