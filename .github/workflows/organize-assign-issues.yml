---
name: "Organize: Assign Issues"

# +-------------------------------------------+
# |                                           |
# |    DO NOT EDIT DIRECTLY !!!!!             |
# |                                           |
# |    File is generated by jarvis.           |
# |    Update file in the jarvis module so    |
# |    you can apply changes to all repos.    |
# |                                           |
# +-------------------------------------------+

on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened

permissions:
  contents: read
  issues: write
  # pull-requests: write

jobs:
  add-to-project:
    name: Add to project
    runs-on: ubuntu-latest
    steps:
      - name: Add to project
        uses: actions/add-to-project@main
        with:
          project-url: https://github.com/users/sebastian-sommerfeld-io/projects/1
          github-token: ${{ secrets.GH_TOKEN_REPO_AND_PROJECT }}

  assign-to-user:
    name: Assign to user
    runs-on: ubuntu-latest
    steps:
      - name: Debug output
        run: echo "${{ github.actor }}"
      - name: Assign to user
        # todo IF moved into "In Progress" -> update triggers too
        uses: pozil/auto-assign-issue@v1
        # https://github.com/marketplace/actions/auto-assign-issue
        with:
          assignees: ${{ github.actor }}
          # assignees: sebastian-sommerfeld-io 
          numOfAssignee: 1
          allowSelfAssign: true
          abortIfPreviousAssignees: true

  on-failure:
    runs-on: ubuntu-latest
    needs: ['add-to-project', 'assign-to-user']
    if: failure()

    steps:
      - name: Send Pipeline Status to Google Chat
        if: always()
        uses: Co-qn/google-chat-notification@releases/v1
        with:
          name: ${{ github.workflow }}
          url: ${{ secrets.GOOGLE_CHAT_WEBHOOK }}
          status: failure
